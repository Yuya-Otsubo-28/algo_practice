#include <bits/stdc++.h>
using namespace std;
#define rep(i, s, n) for (int i = (s); i < (int)(n); i++)

vector<int> bubble_sort(vector<int> a, int n) {

}

int main() {
	int	n;
	vector<int> a(100, 0);

	cin >> n;
	rep(i, 0, 100) cin >> a[i];

	bubble_sort(a, n);



	return (0);
}
